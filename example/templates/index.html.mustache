<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    {{#feedItems}}
      <div>
        <h1>{{title}}</h1>
        <span>{{feed.name}}</span>
        <span>{{feed.tags}}</span>
        <div>
          {{{summary}}}
        </div>
      </div>
    {{/feedItems}}
  </body>
</html>